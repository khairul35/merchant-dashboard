import { WalletProps } from "./index.prop";
import Component from "../index.component";

const WalletBalance = (props: WalletProps) => {
    return (<Component {...props} />)
};

export default WalletBalance;
