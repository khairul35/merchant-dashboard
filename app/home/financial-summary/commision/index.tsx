import Component from "./index.component";
import { CommisionProps } from "./index.prop";

const Commision = (props: CommisionProps) => {
    return (<Component {...props} />);
};

export default Commision;
