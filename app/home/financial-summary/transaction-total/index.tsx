import Component from "./index.component";
import { TransactionTotalProps } from "./index.prop";

const TransactionTotal = (props: TransactionTotalProps) => {
    return (<Component {...props} />)
};

export default TransactionTotal;
